#!/usr/bin/env groovy

common {
  slackChannel = ''
  upstreamProjects = 'confluentinc/common'
  testbreakReporting = false
  downStreamRepos = ["schema-registry", "metadata-service", "kafka-rest", "confluent-security-plugins"]
}
